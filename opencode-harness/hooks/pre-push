#!/bin/bash
# Opencode Pre-push Hook (Garbage Collection)
# This triggers the garbage collector in the background before code is pushed to remote.

echo "Spawning background Opencode Garbage Collection..."
nohup oc-gc >/dev/null 2>&1 &
disown

exit 0
